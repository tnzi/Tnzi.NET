/**
 * `useWorkbenchLayout` re-export. Sunk to `@tnzi/ui` in 0.2.x; admin
 * call-sites keep their existing import path through this barrel.
 */
export { useWorkbenchLayout } from '@tnzi/ui'
export type { UseWorkbenchLayoutReturn } from '@tnzi/ui'
