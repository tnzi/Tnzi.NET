/**
 * `useOverlayTheme` - naive theme for teleported overlays.
 *
 * **Sunk to `@tnzi/ui` on 2026-08-05** alongside the overlay shells that use
 * it; re-exported here so existing imports keep working.
 */
export { useOverlayTheme, useOverlayThemeOverrides } from '@tnzi/ui'
