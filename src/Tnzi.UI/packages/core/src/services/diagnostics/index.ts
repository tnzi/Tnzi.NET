/**
 * @tnzi/core/services/diagnostics
 *
 * Diagnostics Service — admin read access to runtime diagnostics
 * (exceptions ring buffer, controller map, module manifests).
 * Mirrors `Tnzi.AspNetCore/Controllers/DefaultDiagnosticsAdminController`.
 */

export * from './api';
export * from './types';
