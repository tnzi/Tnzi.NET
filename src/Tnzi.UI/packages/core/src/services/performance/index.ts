/**
 * @tnzi/core/services/performance
 *
 * Performance Service — admin read access to runtime request timings
 * (percentile summary, per-endpoint stats, slow-request log).
 * Mirrors `Tnzi.Performance/Controllers/DefaultPerformanceAdminController`.
 */

export * from './api';
export * from './types';
